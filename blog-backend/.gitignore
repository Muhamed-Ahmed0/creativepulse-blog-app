# Node modules
node_modules/

# Log files
*.log
*.log.*

# Environment files (ensure you don't expose sensitive data like passwords, API keys, etc.)
.env

# Build directories (if applicable)
dist/
build/

# Temporary files from IDEs (e.g., VS Code)
.vscode/

# npm-debug log (for npm errors)
npm-debug.log*

# yarn.lock and package-lock.json (optional based on which package manager you use)
yarn.lock
package-lock.json

# OS generated files
.DS_Store  # macOS
Thumbs.db  # Windows

# Optional: Ignore any directories that hold test data
tests/tmp/
